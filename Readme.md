# Docker

- `docker build -t analytics-api -f Dockerfile .`
- `docker run analyics-api`

# docker compose

- `docker compose up`
- `docker compose down` or `docker compose down -v` to remove volume
- `docker compose run app /bin/bash` or `docker compose run app python` # ssh it in to the container
